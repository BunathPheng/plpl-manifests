---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plpl                  # Ingress name
  labels:
    app: plpl
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "plpl.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plpl               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
